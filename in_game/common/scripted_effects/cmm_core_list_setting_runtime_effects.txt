# Rebuild one ordered list after moving the marked row to `local_var:cmm_list_target_index`.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
cmm_reorder_list_setting = {
	clear_variable_list = cmm_list_rebuild_temp
	set_local_variable = { name = cmm_list_source_index value = 0 }
	set_local_variable = { name = cmm_list_found_moving_item value = 0 }
	set_local_variable = { name = cmm_list_marked_position value = var:cmm_list_position_to_apply }

	every_in_list = {
		variable = cmm_list_items_$setting$
		save_scope_as = cmm_list_current_item

		root = {
			add_to_variable_list = {
				name = cmm_list_rebuild_temp
				target = scope:cmm_list_current_item
			}
		}

		if = {
			limit = { local_var:cmm_list_source_index = local_var:cmm_list_marked_position }
			save_scope_as = cmm_list_moving_item
			set_local_variable = { name = cmm_list_found_moving_item value = 1 }
		}

		change_local_variable = { name = cmm_list_source_index add = 1 }
	}

	if = {
		limit = { local_var:cmm_list_found_moving_item = 1 }
		clear_variable_list = cmm_list_items_$setting$
		set_local_variable = { name = cmm_list_reinsert_index value = 0 }
		set_local_variable = { name = cmm_list_inserted_moving_item value = 0 }

		every_in_list = {
			variable = cmm_list_rebuild_temp
			save_scope_as = cmm_list_current_item

			if = {
				limit = { NOT = { this = scope:cmm_list_moving_item } }
				if = {
					limit = {
						AND = {
							local_var:cmm_list_reinsert_index = local_var:cmm_list_target_index
							local_var:cmm_list_inserted_moving_item = 0
						}
					}
					root = {
						add_to_variable_list = {
							name = cmm_list_items_$setting$
							target = scope:cmm_list_moving_item
						}
					}
					set_local_variable = { name = cmm_list_inserted_moving_item value = 1 }
				}

				root = {
					add_to_variable_list = {
						name = cmm_list_items_$setting$
						target = scope:cmm_list_current_item
					}
				}
				change_local_variable = { name = cmm_list_reinsert_index add = 1 }
			}
		}

		if = {
			limit = { local_var:cmm_list_inserted_moving_item = 0 }
			add_to_variable_list = {
				name = cmm_list_items_$setting$
				target = scope:cmm_list_moving_item
			}
		}
	}

	clear_variable_list = cmm_list_rebuild_temp
}

# Prepare the bulk bool-field target value from one item field slot.
# Scope: country
# Args: setting (key), item (number), slot (number)
cmm_list_prepare_bool_field_value_from_slot_item = {
	if = {
		limit = { var:$setting$_item_$item$_field_$slot$ = 1 }
		set_variable = { name = cmm_list_field_value_to_apply value = 0 }
	}
	else = {
		set_variable = { name = cmm_list_field_value_to_apply value = 1 }
	}
}

# Prepare the bulk bool-field target value from one clicked item identity.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
# - item (number): Stable item identity ordinal (`1..20`).
cmm_list_prepare_bool_field_value_from_item = {
	switch = {
		trigger = var:cmm_list_field_slot_to_apply
		0 = { cmm_list_prepare_bool_field_value_from_slot_item = { setting = $setting$ item = $item$ slot = 0 } }
		1 = { cmm_list_prepare_bool_field_value_from_slot_item = { setting = $setting$ item = $item$ slot = 1 } }
		2 = { cmm_list_prepare_bool_field_value_from_slot_item = { setting = $setting$ item = $item$ slot = 2 } }
		3 = { cmm_list_prepare_bool_field_value_from_slot_item = { setting = $setting$ item = $item$ slot = 3 } }
		4 = { cmm_list_prepare_bool_field_value_from_slot_item = { setting = $setting$ item = $item$ slot = 4 } }
	}
}

# Prepare the bulk numeric-field target value from one item field slot.
# Scope: country
# Args: setting (key), item (number), slot (number)
cmm_list_prepare_numeric_field_value_from_slot_item = {
	if = {
		limit = { var:cmm_list_field_$slot$_type_$setting$ = 3 }
		set_variable = { name = cmm_list_field_value_to_apply value = var:$setting$_item_$item$_field_$slot$ }
	}
}

# Prepare the bulk numeric-field target value from one clicked item identity.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
# - item (number): Stable item identity ordinal (`1..20`).
cmm_list_prepare_numeric_field_value_from_item = {
	switch = {
		trigger = var:cmm_list_field_slot_to_apply
		0 = { cmm_list_prepare_numeric_field_value_from_slot_item = { setting = $setting$ item = $item$ slot = 0 } }
		1 = { cmm_list_prepare_numeric_field_value_from_slot_item = { setting = $setting$ item = $item$ slot = 1 } }
		2 = { cmm_list_prepare_numeric_field_value_from_slot_item = { setting = $setting$ item = $item$ slot = 2 } }
		3 = { cmm_list_prepare_numeric_field_value_from_slot_item = { setting = $setting$ item = $item$ slot = 3 } }
		4 = { cmm_list_prepare_numeric_field_value_from_slot_item = { setting = $setting$ item = $item$ slot = 4 } }
	}
}

# Apply the queued numeric markers to one ordered list numeric field slot for one item identity.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
# - item (number): Stable item identity ordinal (`1..20`).
# - slot (number): Field slot index (`0..4`).
cmm_list_apply_numeric_field_slot = {
	if = {
		limit = { AND = { var:cmm_list_field_$slot$_type_$setting$ = 3 has_variable = cmm_numeric_step_delta_to_apply } }
		set_variable = { name = cmm_tmp_numeric_value value = var:$setting$_item_$item$_field_$slot$ }
		set_variable = { name = cmm_tmp_numeric_delta value = var:cmm_list_field_$slot$_step_$setting$ }
		change_variable = { name = cmm_tmp_numeric_delta multiply = var:cmm_numeric_step_delta_to_apply }
		change_variable = { name = cmm_tmp_numeric_value add = var:cmm_tmp_numeric_delta }

		if = {
			limit = { var:cmm_tmp_numeric_value > var:cmm_list_field_$slot$_max_$setting$ }
			set_variable = { name = cmm_tmp_numeric_value value = var:cmm_list_field_$slot$_max_$setting$ }
		}

		if = {
			limit = { var:cmm_tmp_numeric_value < var:cmm_list_field_$slot$_min_$setting$ }
			set_variable = { name = cmm_tmp_numeric_value value = var:cmm_list_field_$slot$_min_$setting$ }
		}

		set_variable = { name = $setting$_item_$item$_field_$slot$ value = var:cmm_tmp_numeric_value }
		remove_variable = cmm_tmp_numeric_value
		remove_variable = cmm_tmp_numeric_delta
	}
	else_if = {
		limit = { AND = { var:cmm_list_field_$slot$_type_$setting$ = 3 has_variable = cmm_numeric_change_to_apply } }
		if = {
			limit = { var:cmm_numeric_change_to_apply > 0 }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = var:cmm_list_field_$slot$_max_$setting$ }
		}
		else_if = {
			limit = { var:cmm_numeric_change_to_apply < 0 }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = var:cmm_list_field_$slot$_min_$setting$ }
		}
	}
}

# Apply the queued numeric markers to one ordered list numeric field on one item identity.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
# - item (number): Stable item identity ordinal (`1..20`).
cmm_list_apply_numeric_field_at_item = {
	switch = {
		trigger = var:cmm_list_field_slot_to_apply
		1 = { cmm_list_apply_numeric_field_slot = { setting = $setting$ item = $item$ slot = 1 } }
		2 = { cmm_list_apply_numeric_field_slot = { setting = $setting$ item = $item$ slot = 2 } }
		3 = { cmm_list_apply_numeric_field_slot = { setting = $setting$ item = $item$ slot = 3 } }
		4 = { cmm_list_apply_numeric_field_slot = { setting = $setting$ item = $item$ slot = 4 } }
		5 = { cmm_list_apply_numeric_field_slot = { setting = $setting$ item = $item$ slot = 5 } }
	}
}

# Apply one ordered list field action to one item field slot.
# Scope: country
# Args: setting (key), item (number), slot (number)
cmm_list_apply_field_slot_at_item = {
	if = {
		limit = { AND = { var:cmm_list_action_to_apply = 5 var:cmm_list_field_$slot$_type_$setting$ = 1 } }
		if = {
			limit = { var:$setting$_item_$item$_field_$slot$ = 1 }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = 0 }
		}
		else = {
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = 1 }
		}
	}
	else_if = {
		limit = { AND = { var:cmm_list_action_to_apply = 6 var:cmm_list_field_$slot$_type_$setting$ = 2 } }
		change_variable = { name = $setting$_item_$item$_field_$slot$ add = 1 }
		if = {
			limit = { var:$setting$_item_$item$_field_$slot$ > var:cmm_list_field_$slot$_dropdown_last_index_$setting$ }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = 1 }
		}
	}
	else_if = {
		limit = { AND = { var:cmm_list_action_to_apply = 7 var:cmm_list_field_$slot$_type_$setting$ = 2 } }
		change_variable = { name = $setting$_item_$item$_field_$slot$ add = -1 }
		if = {
			limit = { var:$setting$_item_$item$_field_$slot$ < 1 }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = var:cmm_list_field_$slot$_dropdown_last_index_$setting$ }
		}
	}
	else_if = {
		limit = { AND = { var:cmm_list_action_to_apply = 8 var:cmm_list_field_$slot$_type_$setting$ = 2 has_variable = cmm_dropdown_index_to_apply } }
		set_variable = { name = $setting$_item_$item$_field_$slot$ value = var:cmm_dropdown_index_to_apply }
		if = {
			limit = { var:$setting$_item_$item$_field_$slot$ < 1 }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = 1 }
		}
		if = {
			limit = { var:$setting$_item_$item$_field_$slot$ > var:cmm_list_field_$slot$_dropdown_last_index_$setting$ }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = var:cmm_list_field_$slot$_dropdown_last_index_$setting$ }
		}
	}
	else_if = {
		limit = { AND = { OR = { var:cmm_list_action_to_apply = 9 var:cmm_list_action_to_apply = 10 var:cmm_list_action_to_apply = 11 } has_variable = cmm_list_field_value_to_apply } }
		set_variable = { name = $setting$_item_$item$_field_$slot$ value = var:cmm_list_field_value_to_apply }
		if = {
			limit = { AND = { var:cmm_list_field_$slot$_type_$setting$ = 1 var:$setting$_item_$item$_field_$slot$ < 0 } }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = 0 }
		}
		if = {
			limit = { AND = { var:cmm_list_field_$slot$_type_$setting$ = 1 var:$setting$_item_$item$_field_$slot$ > 1 } }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = 1 }
		}
		if = {
			limit = { AND = { var:cmm_list_field_$slot$_type_$setting$ = 2 var:$setting$_item_$item$_field_$slot$ < 1 } }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = 1 }
		}
		if = {
			limit = { AND = { var:cmm_list_field_$slot$_type_$setting$ = 2 var:$setting$_item_$item$_field_$slot$ > var:cmm_list_field_$slot$_dropdown_last_index_$setting$ } }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = var:cmm_list_field_$slot$_dropdown_last_index_$setting$ }
		}
		if = {
			limit = { AND = { var:cmm_list_field_$slot$_type_$setting$ = 3 var:$setting$_item_$item$_field_$slot$ < var:cmm_list_field_$slot$_min_$setting$ } }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = var:cmm_list_field_$slot$_min_$setting$ }
		}
		if = {
			limit = { AND = { var:cmm_list_field_$slot$_type_$setting$ = 3 var:$setting$_item_$item$_field_$slot$ > var:cmm_list_field_$slot$_max_$setting$ } }
			set_variable = { name = $setting$_item_$item$_field_$slot$ value = var:cmm_list_field_$slot$_max_$setting$ }
		}
	}
}

# Apply one ordered list field action to one item identity index.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
# - item (number): Stable item identity ordinal (`1..20`).
cmm_list_apply_field_at_item = {
	switch = {
		trigger = var:cmm_list_field_slot_to_apply
		1 = { cmm_list_apply_field_slot_at_item = { setting = $setting$ item = $item$ slot = 1 } }
		2 = { cmm_list_apply_field_slot_at_item = { setting = $setting$ item = $item$ slot = 2 } }
		3 = { cmm_list_apply_field_slot_at_item = { setting = $setting$ item = $item$ slot = 3 } }
		4 = { cmm_list_apply_field_slot_at_item = { setting = $setting$ item = $item$ slot = 4 } }
		5 = { cmm_list_apply_field_slot_at_item = { setting = $setting$ item = $item$ slot = 5 } }
	}
}

# Run the queued list-item action for one stable item identity.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
# - item (number): Stable item identity ordinal (`1..20`).
cmm_list_dispatch_item_action = {
	switch = {
		trigger = var:cmm_list_dispatch_action
		1 = { cmm_list_prepare_bool_field_value_from_item = { setting = $setting$ item = $item$ } }
		2 = { cmm_list_prepare_numeric_field_value_from_item = { setting = $setting$ item = $item$ } }
		3 = { cmm_list_apply_numeric_field_at_item = { setting = $setting$ item = $item$ } }
		4 = { cmm_list_apply_field_at_item = { setting = $setting$ item = $item$ } }
	}
}

# Run the queued list-item action for the currently saved ordered-list entry.
# Scope: country
# Args: setting (key)
cmm_list_dispatch_saved_item_action = {
	if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_1 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 1 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_2 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 2 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_3 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 3 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_4 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 4 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_5 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 5 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_6 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 6 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_7 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 7 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_8 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 8 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_9 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 9 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_10 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 10 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_11 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 11 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_12 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 12 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_13 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 13 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_14 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 14 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_15 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 15 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_16 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 16 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_17 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 17 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_18 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 18 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_19 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 19 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_20 }
		cmm_list_dispatch_item_action = { setting = $setting$ item = 20 }
	}
}

# Run the queued list-item action for the marked visible row.
# Scope: country
# Args: setting (key)
cmm_list_apply_dispatch_to_marked_position = {
	set_local_variable = { name = cmm_list_iter_index value = 0 }
	set_local_variable = { name = cmm_list_marked_position value = var:cmm_list_position_to_apply }

	every_in_list = {
		variable = cmm_list_items_$setting$
		save_scope_as = cmm_list_current_item

		if = {
			limit = { local_var:cmm_list_iter_index = local_var:cmm_list_marked_position }
			root = {
				cmm_list_dispatch_saved_item_action = { setting = $setting$ }
			}
		}

		change_local_variable = { name = cmm_list_iter_index add = 1 }
	}
}

# Run the queued list-item action for every item in the ordered list.
# Scope: country
# Args: setting (key)
cmm_list_apply_dispatch_to_all_items = {
	every_in_list = {
		variable = cmm_list_items_$setting$
		save_scope_as = cmm_list_current_item

		root = {
			cmm_list_dispatch_saved_item_action = { setting = $setting$ }
		}
	}
}

# Apply the prepared bulk field value to every item identity in one list setting.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
cmm_list_apply_field_value_to_all_items = {
	set_variable = { name = cmm_list_dispatch_action value = 4 }
	cmm_list_apply_dispatch_to_all_items = { setting = $setting$ }
}

# Apply the pending list reorder (ordered lists only) or field action and clear its markers.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
cmm_apply_list_change = {
	if = {
		limit = {
			AND = {
				has_variable = cmm_list_position_to_apply
				OR = {
					has_variable = cmm_list_action_to_apply
					has_variable = cmm_numeric_step_delta_to_apply
					has_variable = cmm_numeric_change_to_apply
				}
			}
		}
		if = {
			limit = { AND = { has_variable = cmm_list_action_to_apply var:cmm_setting_list_is_ordered_$setting$ = 1 var:cmm_list_action_to_apply = 1 var:cmm_list_position_to_apply > 0 } }
			set_local_variable = { name = cmm_list_target_index value = { value = var:cmm_list_position_to_apply subtract = 1 } }
			cmm_reorder_list_setting = { setting = $setting$ }
		}
		else_if = {
			limit = { AND = { has_variable = cmm_list_action_to_apply var:cmm_setting_list_is_ordered_$setting$ = 1 var:cmm_list_action_to_apply = 2 var:cmm_list_position_to_apply < var:cmm_setting_list_last_index_$setting$ } }
			set_local_variable = { name = cmm_list_target_index value = { value = var:cmm_list_position_to_apply add = 1 } }
			cmm_reorder_list_setting = { setting = $setting$ }
		}
		else_if = {
			limit = { AND = { has_variable = cmm_list_action_to_apply var:cmm_setting_list_is_ordered_$setting$ = 1 var:cmm_list_action_to_apply = 3 var:cmm_list_position_to_apply > 0 } }
			set_local_variable = { name = cmm_list_target_index value = 0 }
			cmm_reorder_list_setting = { setting = $setting$ }
		}
		else_if = {
			limit = { AND = { has_variable = cmm_list_action_to_apply var:cmm_setting_list_is_ordered_$setting$ = 1 var:cmm_list_action_to_apply = 4 var:cmm_list_position_to_apply < var:cmm_setting_list_last_index_$setting$ } }
			set_local_variable = { name = cmm_list_target_index value = var:cmm_setting_list_last_index_$setting$ }
			cmm_reorder_list_setting = { setting = $setting$ }
		}
		else_if = {
			limit = { AND = { has_variable = cmm_list_action_to_apply var:cmm_list_action_to_apply = 9 has_variable = cmm_list_field_slot_to_apply } }
			set_variable = { name = cmm_list_dispatch_action value = 1 }
			cmm_list_apply_dispatch_to_marked_position = { setting = $setting$ }

			if = {
				limit = { has_variable = cmm_list_field_value_to_apply }
				cmm_list_apply_field_value_to_all_items = { setting = $setting$ }
			}
		}
		else_if = {
			limit = { AND = { has_variable = cmm_list_action_to_apply var:cmm_list_action_to_apply = 10 has_variable = cmm_list_field_slot_to_apply has_variable = cmm_dropdown_index_to_apply } }
			set_variable = { name = cmm_list_field_value_to_apply value = var:cmm_dropdown_index_to_apply }
			cmm_list_apply_field_value_to_all_items = { setting = $setting$ }
		}
		else_if = {
			limit = { AND = { has_variable = cmm_list_action_to_apply var:cmm_list_action_to_apply = 11 has_variable = cmm_list_field_slot_to_apply } }
			set_variable = { name = cmm_list_dispatch_action value = 2 }
			cmm_list_apply_dispatch_to_marked_position = { setting = $setting$ }

			if = {
				limit = { has_variable = cmm_list_field_value_to_apply }
				cmm_list_apply_field_value_to_all_items = { setting = $setting$ }
			}
		}
		else_if = {
			limit = {
				AND = {
					has_variable = cmm_list_field_slot_to_apply
					OR = {
						has_variable = cmm_numeric_step_delta_to_apply
						has_variable = cmm_numeric_change_to_apply
					}
				}
			}
			set_variable = { name = cmm_list_dispatch_action value = 3 }
			cmm_list_apply_dispatch_to_marked_position = { setting = $setting$ }
		}
		else_if = {
			limit = {
				AND = {
					has_variable = cmm_list_action_to_apply
					OR = { var:cmm_list_action_to_apply = 5 var:cmm_list_action_to_apply = 6 var:cmm_list_action_to_apply = 7 var:cmm_list_action_to_apply = 8 }
					has_variable = cmm_list_field_slot_to_apply
				}
			}
			set_variable = { name = cmm_list_dispatch_action value = 4 }
			cmm_list_apply_dispatch_to_marked_position = { setting = $setting$ }
		}
	}

	if = {
		limit = { has_variable = cmm_list_position_to_apply }
		remove_variable = cmm_list_position_to_apply
	}

	if = {
		limit = { has_variable = cmm_list_action_to_apply }
		remove_variable = cmm_list_action_to_apply
	}

	if = {
		limit = { has_variable = cmm_list_field_slot_to_apply }
		remove_variable = cmm_list_field_slot_to_apply
	}

	if = {
		limit = { has_variable = cmm_dropdown_index_to_apply }
		remove_variable = cmm_dropdown_index_to_apply
	}

	if = {
		limit = { has_variable = cmm_list_field_value_to_apply }
		remove_variable = cmm_list_field_value_to_apply
	}

	if = {
		limit = { has_variable = cmm_numeric_step_delta_to_apply }
		remove_variable = cmm_numeric_step_delta_to_apply
	}

	if = {
		limit = { has_variable = cmm_numeric_change_to_apply }
		remove_variable = cmm_numeric_change_to_apply
	}

	if = {
		limit = { has_variable = cmm_list_dispatch_action }
		remove_variable = cmm_list_dispatch_action
	}
}

# Iterate over every item in a list setting and call a user-provided effect for each.
# The effect receives `$i$` as the resolved item number (1-20), so the caller can
# use it directly in variable names, e.g. `var:mymod__my_list_item_$i$_field_1`.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
# - effect (key): Name of the scripted effect to call per item.
cmm_for_each_list_item = {
	every_in_list = {
		variable = cmm_list_items_$setting$
		save_scope_as = cmm_list_current_item

		root = {
			cmm_resolve_list_item_and_call = { setting = $setting$ effect = $effect$ }
		}
	}
}

# Internal dispatcher: resolve the current saved item flag to its item number,
# then call the user-provided effect with that number.
# Scope: country
# Args:
# - setting (key): Composite setting key.
# - effect (key): Name of the scripted effect to call.
cmm_resolve_list_item_and_call = {
	if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_1 }
		$effect$ = { setting = $setting$ i = 1 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_2 }
		$effect$ = { setting = $setting$ i = 2 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_3 }
		$effect$ = { setting = $setting$ i = 3 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_4 }
		$effect$ = { setting = $setting$ i = 4 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_5 }
		$effect$ = { setting = $setting$ i = 5 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_6 }
		$effect$ = { setting = $setting$ i = 6 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_7 }
		$effect$ = { setting = $setting$ i = 7 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_8 }
		$effect$ = { setting = $setting$ i = 8 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_9 }
		$effect$ = { setting = $setting$ i = 9 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_10 }
		$effect$ = { setting = $setting$ i = 10 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_11 }
		$effect$ = { setting = $setting$ i = 11 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_12 }
		$effect$ = { setting = $setting$ i = 12 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_13 }
		$effect$ = { setting = $setting$ i = 13 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_14 }
		$effect$ = { setting = $setting$ i = 14 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_15 }
		$effect$ = { setting = $setting$ i = 15 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_16 }
		$effect$ = { setting = $setting$ i = 16 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_17 }
		$effect$ = { setting = $setting$ i = 17 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_18 }
		$effect$ = { setting = $setting$ i = 18 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_19 }
		$effect$ = { setting = $setting$ i = 19 }
	}
	else_if = {
		limit = { scope:cmm_list_current_item = flag:$setting$_item_20 }
		$effect$ = { setting = $setting$ i = 20 }
	}
}
