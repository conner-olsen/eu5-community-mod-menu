# Quantize a slider setting value to the nearest configured step.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
cmm_quantize_slider_setting = {
	if = {
		limit = { var:cmm_setting_is_global_$setting$ = 1 }

		set_variable = { name = cmm_tmp_slider_value value = global_var:$setting$ }
		change_variable = { name = cmm_tmp_slider_value subtract = var:cmm_setting_min_$setting$ }
		change_variable = { name = cmm_tmp_slider_value divide = var:cmm_setting_step_$setting$ }
		round_variable = {
			name = cmm_tmp_slider_value
			nearest = 1
		}
		change_variable = { name = cmm_tmp_slider_value multiply = var:cmm_setting_step_$setting$ }
		change_variable = { name = cmm_tmp_slider_value add = var:cmm_setting_min_$setting$ }

		if = {
			limit = { var:cmm_tmp_slider_value < var:cmm_setting_min_$setting$ }
			set_variable = { name = cmm_tmp_slider_value value = var:cmm_setting_min_$setting$ }
		}

		if = {
			limit = { var:cmm_tmp_slider_value > var:cmm_setting_max_$setting$ }
			set_variable = { name = cmm_tmp_slider_value value = var:cmm_setting_max_$setting$ }
		}

		set_global_variable = { name = $setting$ value = var:cmm_tmp_slider_value }
		remove_variable = cmm_tmp_slider_value
	}
	else = {
		set_variable = { name = cmm_tmp_slider_value value = var:$setting$ }
		change_variable = { name = cmm_tmp_slider_value subtract = var:cmm_setting_min_$setting$ }
		change_variable = { name = cmm_tmp_slider_value divide = var:cmm_setting_step_$setting$ }
		round_variable = {
			name = cmm_tmp_slider_value
			nearest = 1
		}
		change_variable = { name = cmm_tmp_slider_value multiply = var:cmm_setting_step_$setting$ }
		change_variable = { name = cmm_tmp_slider_value add = var:cmm_setting_min_$setting$ }

		if = {
			limit = { var:cmm_tmp_slider_value < var:cmm_setting_min_$setting$ }
			set_variable = { name = cmm_tmp_slider_value value = var:cmm_setting_min_$setting$ }
		}

		if = {
			limit = { var:cmm_tmp_slider_value > var:cmm_setting_max_$setting$ }
			set_variable = { name = cmm_tmp_slider_value value = var:cmm_setting_max_$setting$ }
		}

		set_variable = { name = $setting$ value = var:cmm_tmp_slider_value }
		remove_variable = cmm_tmp_slider_value
	}
}

# Register a slider setting for a mod (idempotent by mod_id + setting_id).
# Scope: country
# Args:
# - mod_id (key): Owner mod id.
# - setting_id (key): Setting id unique within mod.
# - tab_id (key): Owner tab id within mod.
# - default_value (number): Initial value; applied when the setting does not exist yet.
# - min_value (number): Lower clamp bound.
# - max_value (number): Upper clamp bound.
# - step_value (number): Logical slider step size.
cmm_register_slider_setting = {
	set_variable = { name = cmm_setting_is_global_$mod_id$__$setting_id$ value = 0 }
	set_variable = { name = cmm_setting_is_slider_$mod_id$__$setting_id$ value = 1 }
	set_variable = { name = cmm_setting_is_button_$mod_id$__$setting_id$ value = 0 }
	set_variable = { name = cmm_setting_min_$mod_id$__$setting_id$ value = $min_value$ }
	set_variable = { name = cmm_setting_max_$mod_id$__$setting_id$ value = $max_value$ }
	set_variable = { name = cmm_setting_step_$mod_id$__$setting_id$ value = $step_value$ }

	set_variable = { name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ value = $max_value$ }
	change_variable = { name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ subtract = $min_value$ }
	change_variable = { name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ divide = $step_value$ }
	round_variable = {
		name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$
		nearest = 1
	}
	change_variable = { name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ add = 1 }

	if = {
		limit = { var:cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ < 2 }
		set_variable = { name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ value = 2 }
	}

	set_variable = { name = cmm_setting_slider_visual_step_count_$mod_id$__$setting_id$ value = var:cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ }

	if = {
		limit = { var:cmm_setting_slider_visual_step_count_$mod_id$__$setting_id$ > 25 }
		set_variable = { name = cmm_setting_slider_visual_step_count_$mod_id$__$setting_id$ value = 25 }
	}

	set_variable = { name = cmm_setting_slider_actual_last_index_$mod_id$__$setting_id$ value = var:cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ }
	change_variable = { name = cmm_setting_slider_actual_last_index_$mod_id$__$setting_id$ add = -1 }
	set_variable = { name = cmm_setting_slider_visual_last_index_$mod_id$__$setting_id$ value = var:cmm_setting_slider_visual_step_count_$mod_id$__$setting_id$ }
	change_variable = { name = cmm_setting_slider_visual_last_index_$mod_id$__$setting_id$ add = -1 }

	cmm_register_setting_metadata = {
		mod_id = $mod_id$
		setting_id = $setting_id$
		tab_id = $tab_id$
	}

	if = {
		limit = { NOT = { has_variable = $mod_id$__$setting_id$ } }
		set_variable = { name = $mod_id$__$setting_id$ value = $default_value$ }
	}

	if = {
		limit = { var:$mod_id$__$setting_id$ < $min_value$ }
		set_variable = { name = $mod_id$__$setting_id$ value = $min_value$ }
	}

	if = {
		limit = { var:$mod_id$__$setting_id$ > $max_value$ }
		set_variable = { name = $mod_id$__$setting_id$ value = $max_value$ }
	}

	cmm_quantize_slider_setting = {
		setting = $mod_id$__$setting_id$
	}

	# Benign warning suppression
	if = {
		limit = {
			always = no
			exists = var:cmm_setting_is_global_$mod_id$__$setting_id$
			exists = var:cmm_setting_is_slider_$mod_id$__$setting_id$
			exists = var:cmm_setting_is_button_$mod_id$__$setting_id$
			exists = var:cmm_setting_min_$mod_id$__$setting_id$
			exists = var:cmm_setting_max_$mod_id$__$setting_id$
			exists = var:cmm_setting_step_$mod_id$__$setting_id$
			exists = var:cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$
			exists = var:cmm_setting_slider_visual_step_count_$mod_id$__$setting_id$
			exists = var:cmm_setting_slider_actual_last_index_$mod_id$__$setting_id$
			exists = var:cmm_setting_slider_visual_last_index_$mod_id$__$setting_id$
			exists = var:$mod_id$__$setting_id$
		}
	}
}

# Register a global slider setting for a mod.
# Scope: country
# Args:
# - mod_id (key): Owner mod id.
# - setting_id (key): Setting id unique within mod.
# - tab_id (key): Owner tab id within mod.
# - default_value (number): Initial global value when missing.
# - min_value (number): Lower clamp bound.
# - max_value (number): Upper clamp bound.
# - step_value (number): Logical slider step size.
cmm_register_global_slider_setting = {
	set_variable = { name = cmm_setting_is_global_$mod_id$__$setting_id$ value = 1 }
	set_variable = { name = cmm_setting_is_slider_$mod_id$__$setting_id$ value = 1 }
	set_variable = { name = cmm_setting_is_button_$mod_id$__$setting_id$ value = 0 }
	set_variable = { name = cmm_setting_min_$mod_id$__$setting_id$ value = $min_value$ }
	set_variable = { name = cmm_setting_max_$mod_id$__$setting_id$ value = $max_value$ }
	set_variable = { name = cmm_setting_step_$mod_id$__$setting_id$ value = $step_value$ }

	set_variable = { name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ value = $max_value$ }
	change_variable = { name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ subtract = $min_value$ }
	change_variable = { name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ divide = $step_value$ }
	round_variable = {
		name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$
		nearest = 1
	}
	change_variable = { name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ add = 1 }

	if = {
		limit = { var:cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ < 2 }
		set_variable = { name = cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ value = 2 }
	}

	set_variable = { name = cmm_setting_slider_visual_step_count_$mod_id$__$setting_id$ value = var:cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ }

	if = {
		limit = { var:cmm_setting_slider_visual_step_count_$mod_id$__$setting_id$ > 25 }
		set_variable = { name = cmm_setting_slider_visual_step_count_$mod_id$__$setting_id$ value = 25 }
	}

	set_variable = { name = cmm_setting_slider_actual_last_index_$mod_id$__$setting_id$ value = var:cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$ }
	change_variable = { name = cmm_setting_slider_actual_last_index_$mod_id$__$setting_id$ add = -1 }
	set_variable = { name = cmm_setting_slider_visual_last_index_$mod_id$__$setting_id$ value = var:cmm_setting_slider_visual_step_count_$mod_id$__$setting_id$ }
	change_variable = { name = cmm_setting_slider_visual_last_index_$mod_id$__$setting_id$ add = -1 }

	cmm_register_setting_metadata = {
		mod_id = $mod_id$
		setting_id = $setting_id$
		tab_id = $tab_id$
	}

	if = {
		limit = { NOT = { has_global_variable = $mod_id$__$setting_id$ } }
		set_global_variable = { name = $mod_id$__$setting_id$ value = $default_value$ }
	}

	if = {
		limit = { global_var:$mod_id$__$setting_id$ < $min_value$ }
		set_global_variable = { name = $mod_id$__$setting_id$ value = $min_value$ }
	}

	if = {
		limit = { global_var:$mod_id$__$setting_id$ > $max_value$ }
		set_global_variable = { name = $mod_id$__$setting_id$ value = $max_value$ }
	}

	cmm_quantize_slider_setting = {
		setting = $mod_id$__$setting_id$
	}

	# Benign warning suppression
	if = {
		limit = {
			always = no
			exists = var:cmm_setting_is_global_$mod_id$__$setting_id$
			exists = var:cmm_setting_is_slider_$mod_id$__$setting_id$
			exists = var:cmm_setting_is_button_$mod_id$__$setting_id$
			exists = var:cmm_setting_min_$mod_id$__$setting_id$
			exists = var:cmm_setting_max_$mod_id$__$setting_id$
			exists = var:cmm_setting_step_$mod_id$__$setting_id$
			exists = var:cmm_setting_slider_actual_step_count_$mod_id$__$setting_id$
			exists = var:cmm_setting_slider_visual_step_count_$mod_id$__$setting_id$
			exists = var:cmm_setting_slider_actual_last_index_$mod_id$__$setting_id$
			exists = var:cmm_setting_slider_visual_last_index_$mod_id$__$setting_id$
			exists = global_var:$mod_id$__$setting_id$
		}
	}
}

# Set a slider setting directly from the pending visual-track index and clamp to range.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
cmm_set_slider_setting_from_visual_index = {
	set_variable = { name = cmm_tmp_slider_actual_index value = var:cmm_slider_index_to_apply }
	change_variable = { name = cmm_tmp_slider_actual_index multiply = var:cmm_setting_slider_actual_last_index_$setting$ }
	change_variable = { name = cmm_tmp_slider_actual_index divide = var:cmm_setting_slider_visual_last_index_$setting$ }
	round_variable = {
		name = cmm_tmp_slider_actual_index
		nearest = 1
	}

	set_variable = { name = cmm_tmp_slider_value value = var:cmm_setting_step_$setting$ }
	change_variable = { name = cmm_tmp_slider_value multiply = var:cmm_tmp_slider_actual_index }
	change_variable = { name = cmm_tmp_slider_value add = var:cmm_setting_min_$setting$ }

	if = {
		limit = { var:cmm_tmp_slider_value < var:cmm_setting_min_$setting$ }
		set_variable = { name = cmm_tmp_slider_value value = var:cmm_setting_min_$setting$ }
	}

	if = {
		limit = { var:cmm_tmp_slider_value > var:cmm_setting_max_$setting$ }
		set_variable = { name = cmm_tmp_slider_value value = var:cmm_setting_max_$setting$ }
	}

	if = {
		limit = { var:cmm_setting_is_global_$setting$ = 1 }
		set_global_variable = { name = $setting$ value = var:cmm_tmp_slider_value }
	}
	else = {
		set_variable = { name = $setting$ value = var:cmm_tmp_slider_value }
	}

	remove_variable = cmm_tmp_slider_actual_index
	remove_variable = cmm_tmp_slider_value
}

# Apply the pending slider track selection or numeric step marker to a slider setting.
# Scope: country
# Args:
# - setting (key): Composite setting key `<mod_id>__<setting_id>`.
cmm_apply_slider_change = {
	if = {
		limit = { has_variable = cmm_slider_index_to_apply }

		cmm_set_slider_setting_from_visual_index = {
			setting = $setting$
		}

		remove_variable = cmm_slider_index_to_apply

		if = {
			limit = { has_variable = cmm_numeric_change_to_apply }
			remove_variable = cmm_numeric_change_to_apply
		}
	}
	else = {
		cmm_apply_numeric_change = {
			setting = $setting$
		}
	}
}
