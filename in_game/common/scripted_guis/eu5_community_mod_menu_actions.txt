CMM_ToggleExampleSetting = {
	effect = {
		cmm_toggle_bool_setting = {
			setting_key = cmm_core__example_toggle
		}
	}
}

CMM_RegisterCoreMod = {
	effect = {
		cmm_register_mod = {
			mod_id = cmm_core
			display_name_key = CMM_CORE_MOD_NAME
			description_key = CMM_CORE_MOD_DESC
		}

		cmm_register_bool_setting = {
			mod_id = cmm_core
			setting_id = example_toggle
			display_name_key = CMM_CORE_BOOL_EXAMPLE_NAME
			description_key = CMM_CORE_BOOL_EXAMPLE_DESC
			default_value = 0
		}

		# Static analyzer wiring: read the core dirty variable in executed logic.
		if = {
			limit = { has_variable = cmm_setting_dirty_cmm_core__example_toggle }
			change_variable = { name = cmm_registered_mod_count add = 0 }
		}
	}
}


