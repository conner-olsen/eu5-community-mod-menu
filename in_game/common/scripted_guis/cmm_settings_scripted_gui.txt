# Register core CMM metadata and run the registration hook.
# Scope: country
# Args: none
CMM_RegisterCoreMod = {
	effect = {
		cmm_register_menu_settings = yes
		trigger_event_silently = {
			on_action = cmm_on_mod_registration
		}
	}
}

# Handle example toggle checkbox clicks.
# Scope: country
# Args: none
cmm_core__example_toggle_on_changed = {
	scope = country
	effect = {
		cmm_toggle_bool_setting = {
			setting = cmm_core__example_toggle
		}

		if = {
			limit = { var:cmm_core__example_toggle = 1 }
			# enabled effect
		}
		else = {
			# disabled effect
		}
	}
}

# Handle example global toggle checkbox clicks.
# Scope: country
# Args: none
cmm_core__global_example_toggle_on_changed = {
	scope = country
	effect = {
		cmm_toggle_bool_setting = {
			setting = cmm_core__global_example_toggle
		}

		if = {
			limit = { var:cmm_core__global_example_toggle = 1 }
			# enabled effect
		}
		else = {
			# disabled effect
		}
	}
}

# Run the core example button action.
# Scope: country
# Args: none
cmm_core__example_button_on_changed = {
	scope = country
	effect = {
		# Button effect
	}
}

# On-changed callback for the core example numeric setting.
# Scope: country
# Args: none
cmm_core__example_numeric_on_changed = {
	scope = country
	effect = {
		cmm_apply_numeric_change = {
			setting = cmm_core__example_numeric
		}
		# optional custom logic
	}
}

# On-changed callback for the core example slider setting.
# Scope: country
# Args: none
cmm_core__example_slider_on_changed = {
	scope = country
	effect = {
		cmm_apply_slider_change = {
			setting = cmm_core__example_slider
		}
		# optional custom logic
	}
}

# On-changed callback for the core example dropdown setting.
# Scope: country
# Args: none
cmm_core__example_dropdown_on_changed = {
	scope = country
	effect = {
		cmm_apply_dropdown_change = {
			setting = cmm_core__example_dropdown
		}
		# optional custom logic
	}
}
