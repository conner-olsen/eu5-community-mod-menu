# Args: Name (string)
# Returns: variable
macro = {
    description = "Country variable accessor"
    definition = "CMMVar(Name)"
    replace_with = "Player.MakeScope.GetVariable(Name)"
}

# Args: Prefix (string), Key (string)
# Returns: variable
macro = {
    description = "Metadata variable accessor"
    definition = "CMMMetadataVar(Prefix,Key)"
    replace_with = "CMMVar(Concatenate(Prefix, Key))"
}

# Args: Prefix (string), Key (string)
# Returns: string
macro = {
    description = "Metadata flag-name accessor"
    definition = "CMMMetadataFlag(Prefix,Key)"
    replace_with = "CMMMetadataVar(Prefix, Key).GetFlagName"
}

# Args: Prefix (string), Key (string)
# Returns: value
macro = {
    description = "Metadata value accessor"
    definition = "CMMMetadataValue(Prefix,Key)"
    replace_with = "CMMMetadataVar(Prefix, Key).GetValue"
}

# Args: Prefix (string), Key (string)
# Returns: int32
macro = {
    description = "Metadata int accessor"
    definition = "CMMMetadataInt(Prefix,Key)"
    replace_with = "FixedPointToInt(CMMMetadataValue(Prefix, Key))"
}

# Args: Prefix (string), Key (string)
# Returns: boolean
macro = {
    description = "True when one metadata key is set"
    definition = "CMMMetadataIsSet(Prefix,Key)"
    replace_with = "CMMMetadataVar(Prefix, Key).IsSet"
}

# Args: Value (value)
# Returns: boolean
macro = {
    description = "True when one numeric value equals 1"
    definition = "CMMValueEqualsOne(Value)"
    replace_with = "EqualTo_CFixedPoint(Value, '(CFixedPoint)1')"
}

# Args: Prefix (string), Key (string)
# Returns: boolean
macro = {
    description = "True when one metadata value equals 1"
    definition = "CMMMetadataEqualsOne(Prefix,Key)"
    replace_with = "CMMValueEqualsOne(CMMMetadataValue(Prefix, Key))"
}

# Args: Key (string), Suffix (string)
# Returns: string
macro = {
    description = "Localization lookup for one runtime key plus suffix"
    definition = "CMMLocalizedSuffix(Key,Suffix)"
    replace_with = "CMMVar(Concatenate(Key, Suffix)).GetFlagName"
}

# Args: OptionRoot (string), Index (int32)
# Returns: string
macro = {
    description = "Localized dropdown option label"
    definition = "CMMOptionLabel(OptionRoot,Index)"
    replace_with = "Localize(Concatenate(Concatenate(OptionRoot, '_option_'), Concatenate(ToString_int32(Index), '_name')))"
}

# Args: Name (string)
# Returns: boolean
macro = {
    description = "True when one variable-system key exists"
    definition = "CMMSelectedValueExists(Name)"
    replace_with = "GetVariableSystem.Exists(Name)"
}

# Args: Name (string), Value (string)
# Returns: boolean
macro = {
    description = "True when one variable-system key exists and matches one value"
    definition = "CMMSelectedHasValue(Name,Value)"
    replace_with = "And(CMMSelectedValueExists(Name), GetVariableSystem.HasValue(Name, Value))"
}

# Args: Name (string)
# Returns: none
macro = {
    description = "Execute one scripted GUI callback"
    definition = "CMMExecuteGui(Name)"
    replace_with = "GetScriptedGui(Name).Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: Prefix (string), Suffix (string)
# Returns: none
macro = {
    description = "Execute one scripted GUI callback resolved from prefix plus suffix"
    definition = "CMMExecuteGuiSuffix(Prefix,Suffix)"
    replace_with = "CMMExecuteGui(Concatenate(Prefix, Suffix))"
}

# Args: Name (string)
# Returns: boolean
macro = {
    description = "True when one scripted GUI callback is shown"
    definition = "CMMGuiIsShown(Name)"
    replace_with = "GetScriptedGui(Name).IsShown(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when selected mod variable-system key exists"
    definition = "CMMSelectedModExists"
    replace_with = "CMMSelectedValueExists('cmm_selected_mod')"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when selected tab variable-system key exists"
    definition = "CMMSelectedTabExists"
    replace_with = "CMMSelectedValueExists('cmm_selected_tab')"
}

# Args: ModId (string), TabKey (string)
# Returns: boolean
macro = {
    description = "True when one mod id and tab key match the current selection"
    definition = "CMMMatchesSelectedModAndTab(ModId,TabKey)"
    replace_with = "And(CMMSelectedHasValue('cmm_selected_mod', ModId), CMMSelectedHasValue('cmm_selected_tab', TabKey))"
}

# Args: GroupKey (string)
# Returns: boolean
macro = {
    description = "Visibility rule for one group row"
    definition = "CMMGroupRowVisible(GroupKey)"
    replace_with = "CMMMatchesSelectedModAndTab(CMMMetadataFlag('cmm_group_owner_mod_id_', GroupKey), CMMMetadataFlag('cmm_group_owner_tab_key_', GroupKey))"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when no mods are registered yet"
    definition = "CMMNoModsRegistered"
    replace_with = "IsDataModelEmpty(Player.MakeScope.GetList('cmm_registered_mod_ids'))"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when at least one mod is registered"
    definition = "CMMHasRegisteredMods"
    replace_with = "Not(IsDataModelEmpty(Player.MakeScope.GetList('cmm_registered_mod_ids')))"
}

# Args: ModId (string)
# Returns: boolean
macro = {
    description = "Filter match for one mod row against current search text"
    definition = "CMMModMatchesSearch(ModId)"
    replace_with = "Or4(Not(CMMSelectedValueExists('cmm_mod_search_text')), StringIsEmpty(GetVariableSystem.Get('cmm_mod_search_text')), StringContains(CMMLocalizedSuffix(ModId, '_name'), GetVariableSystem.Get('cmm_mod_search_text')), StringContains(ModId, GetVariableSystem.Get('cmm_mod_search_text')))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as global"
    definition = "CMMSettingIsGlobal(SettingKey)"
    replace_with = "CMMMetadataEqualsOne('cmm_setting_is_global_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as text type"
    definition = "CMMSettingIsText(SettingKey)"
    replace_with = "CMMMetadataIsSet('cmm_setting_text_character_limit_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as slider type"
    definition = "CMMSettingIsSlider(SettingKey)"
    replace_with = "CMMMetadataEqualsOne('cmm_setting_is_slider_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as button type"
    definition = "CMMSettingIsButton(SettingKey)"
    replace_with = "CMMMetadataEqualsOne('cmm_setting_is_button_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as list type"
    definition = "CMMSettingIsList(SettingKey)"
    replace_with = "CMMMetadataIsSet('cmm_setting_list_count_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when local player can edit this setting"
    definition = "CMMCanEditSetting(SettingKey)"
    replace_with = "Or(Not(CMMSettingIsGlobal(SettingKey)), And(Or(EqualTo_string(SettingKey, 'cmm_core__enable_host_only_tools'), And(GetGlobalVariable('cmm_core__enable_host_only_tools').IsSet, CMMValueEqualsOne(GetGlobalVariable('cmm_core__enable_host_only_tools').GetValue))), Or(Not(GameIsMultiplayer), IsHost)))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as dropdown type"
    definition = "CMMSettingIsDropdown(SettingKey)"
    replace_with = "CMMMetadataIsSet('cmm_setting_dropdown_count_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as numeric type"
    definition = "CMMSettingIsNumeric(SettingKey)"
    replace_with = "And(CMMMetadataIsSet('cmm_setting_step_', SettingKey), Not(CMMSettingIsSlider(SettingKey)))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as bool type"
    definition = "CMMSettingIsBool(SettingKey)"
    replace_with = "And(And4(Not(CMMSettingIsNumeric(SettingKey)), Not(CMMSettingIsDropdown(SettingKey)), Not(CMMSettingIsSlider(SettingKey)), Not(CMMSettingIsText(SettingKey))), And(Not(CMMSettingIsButton(SettingKey)), Not(CMMSettingIsList(SettingKey))))"
}

# Args: SettingKey (string)
# Returns: value
macro = {
    description = "Read current setting value from country or global scope"
    definition = "CMMSettingValue(SettingKey)"
    replace_with = "Select_CFixedPoint(CMMSettingIsGlobal(SettingKey), GetGlobalVariable(SettingKey).GetValue, CMMVar(SettingKey).GetValue)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Visibility rule for one setting row"
    definition = "CMMSettingRowVisible(SettingKey)"
    replace_with = "And(CMMMatchesSelectedModAndTab(CMMMetadataFlag('cmm_setting_owner_mod_id_', SettingKey), CMMMetadataFlag('cmm_setting_owner_tab_key_', SettingKey)), Or(CMMSettingIsText(SettingKey), CMMGuiIsShown(Concatenate(SettingKey, '_on_changed'))))"
}

# Args: SettingKey (string)
# Returns: none
macro = {
    description = "Execute setting on-changed scripted GUI callback"
    definition = "CMMExecuteSettingChanged(SettingKey)"
    replace_with = "CMMExecuteGui(Concatenate(SettingKey, '_on_changed'))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Checkbox checked state for bool settings (1 = on)"
    definition = "CMMIsSettingEnabled(SettingKey)"
    replace_with = "CMMValueEqualsOne(CMMSettingValue(SettingKey))"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Dropdown option count for one setting"
    definition = "CMMDropdownOptionCount(SettingKey)"
    replace_with = "CMMMetadataInt('cmm_setting_dropdown_count_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Dropdown selected index for one setting"
    definition = "CMMDropdownSelectedIndex(SettingKey)"
    replace_with = "FixedPointToInt(CMMSettingValue(SettingKey))"
}

# Args: none
# Returns: boolean
macro = {
    description = "Show empty-state text when selected tab has zero visible settings"
    definition = "CMMShowNoSettingsForSelectedTab"
    replace_with = "And(CMMSelectedModExists, Or3(Not(CMMSelectedTabExists), Not(CMMMetadataIsSet('cmm_tab_setting_count_', GetVariableSystem.Get('cmm_selected_tab'))), EqualTo_CFixedPoint(CMMMetadataValue('cmm_tab_setting_count_', GetVariableSystem.Get('cmm_selected_tab')), '(CFixedPoint)0')))"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when text settings may be edited in the current session"
    definition = "CMMCanEditTextSetting"
    replace_with = "Not(GameIsMultiplayer)"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Text setting editbox character limit"
    definition = "CMMTextSettingCharacterLimit(SettingKey)"
    replace_with = "CMMMetadataInt('cmm_setting_text_character_limit_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when submitted text should be wrapped in double quotes"
    definition = "CMMTextSettingQuoteText(SettingKey)"
    replace_with = "CMMMetadataEqualsOne('cmm_setting_text_quote_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Variable-system cache key for one text setting input field"
    definition = "CMMTextSettingInputCacheKey(SettingKey)"
    replace_with = "Concatenate('cmm_text_input_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Scripted effect callback key for one text setting"
    definition = "CMMTextSettingCallbackKey(SettingKey)"
    replace_with = "Concatenate(SettingKey, '_on_changed')"
}

# Args: ItemKey (string)
# Returns: string
macro = {
    description = "Owner setting key for one ordered list item key"
    definition = "CMMListItemOwnerSettingKey(ItemKey)"
    replace_with = "CMMMetadataFlag('cmm_list_item_owner_setting_', ItemKey)"
}

# Args: ItemKey (string)
# Returns: boolean
macro = {
    description = "True when local player can edit the owner setting for one ordered list item"
    definition = "CMMCanEditListItemSetting(ItemKey)"
    replace_with = "CMMCanEditSetting(CMMListItemOwnerSettingKey(ItemKey))"
}

# Args: ItemKey (string)
# Returns: none
macro = {
    description = "Execute the owner setting callback for one ordered list item"
    definition = "CMMExecuteListItemSettingChanged(ItemKey)"
    replace_with = "CMMExecuteSettingChanged(CMMListItemOwnerSettingKey(ItemKey))"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Ordered list field count for one setting"
    definition = "CMMListFieldCount(SettingKey)"
    replace_with = "CMMMetadataInt('cmm_list_field_count_', SettingKey)"
}

# Args: SettingKey (string), SlotKey (string), Suffix (string)
# Returns: variable
macro = {
    description = "Ordered list field metadata variable accessor"
    definition = "CMMListFieldMetadataVar(SettingKey,SlotKey,Suffix)"
    replace_with = "CMMVar(Concatenate(Concatenate('cmm_list_field_', SlotKey), Concatenate(Suffix, SettingKey)))"
}

# Args: SettingKey (string), SlotKey (string), Suffix (string)
# Returns: string
macro = {
    description = "Ordered list field metadata flag-name accessor"
    definition = "CMMListFieldMetadataFlag(SettingKey,SlotKey,Suffix)"
    replace_with = "CMMListFieldMetadataVar(SettingKey, SlotKey, Suffix).GetFlagName"
}

# Args: SettingKey (string), SlotKey (string), Suffix (string)
# Returns: value
macro = {
    description = "Ordered list field metadata value accessor"
    definition = "CMMListFieldMetadataValue(SettingKey,SlotKey,Suffix)"
    replace_with = "CMMListFieldMetadataVar(SettingKey, SlotKey, Suffix).GetValue"
}

# Args: SettingKey (string), SlotKey (string), Suffix (string)
# Returns: int32
macro = {
    description = "Ordered list field metadata int accessor"
    definition = "CMMListFieldMetadataInt(SettingKey,SlotKey,Suffix)"
    replace_with = "FixedPointToInt(CMMListFieldMetadataValue(SettingKey, SlotKey, Suffix))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: variable
macro = {
    description = "Ordered list item field variable accessor"
    definition = "CMMListItemFieldVar(ItemKey,SlotKey)"
    replace_with = "CMMVar(Concatenate(ItemKey, Concatenate('_field_', SlotKey)))"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: value
macro = {
    description = "Ordered list item field value accessor"
    definition = "CMMListItemFieldValue(ItemKey,SlotKey)"
    replace_with = "CMMListItemFieldVar(ItemKey, SlotKey).GetValue"
}

# Args: ItemKey (string), SlotKey (string)
# Returns: int32
macro = {
    description = "Ordered list item field int accessor"
    definition = "CMMListItemFieldInt(ItemKey,SlotKey)"
    replace_with = "FixedPointToInt(CMMListItemFieldValue(ItemKey, SlotKey))"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when one ordered list field slot is bool"
    definition = "CMMListFieldIsBool(SettingKey,SlotKey)"
    replace_with = "EqualTo_CFixedPoint(CMMListFieldMetadataValue(SettingKey, SlotKey, '_type_'), '(CFixedPoint)1')"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when one ordered list field slot is dropdown"
    definition = "CMMListFieldIsDropdown(SettingKey,SlotKey)"
    replace_with = "EqualTo_CFixedPoint(CMMListFieldMetadataValue(SettingKey, SlotKey, '_type_'), '(CFixedPoint)2')"
}

# Args: SettingKey (string), SlotKey (string)
# Returns: boolean
macro = {
    description = "True when one ordered list field slot is numeric"
    definition = "CMMListFieldIsNumeric(SettingKey,SlotKey)"
    replace_with = "EqualTo_CFixedPoint(CMMListFieldMetadataValue(SettingKey, SlotKey, '_type_'), '(CFixedPoint)3')"
}
