# Args: Name (string)
# Returns: variable
macro = {
    description = "Country variable accessor"
    definition = "CMMVar(Name)"
    replace_with = "Player.MakeScope.GetVariable(Name)"
}

# Args: ModId (string)
# Returns: string
macro = {
    description = "Default tab key for a mod id"
    definition = "CMMDefaultTab(ModId)"
    replace_with = "CMMVar(Concatenate('cmm_mod_default_tab_', ModId)).GetFlagName"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when selected mod variable-system key exists"
    definition = "CMMSelectedModExists"
    replace_with = "GetVariableSystem.Exists('cmm_selected_mod')"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when selected tab variable-system key exists"
    definition = "CMMSelectedTabExists"
    replace_with = "GetVariableSystem.Exists('cmm_selected_tab')"
}

# Args: none
# Returns: string
macro = {
    description = "Selected mod name localization"
    definition = "CMMSelectedModName"
    replace_with = "CMMVar(Concatenate(GetVariableSystem.Get('cmm_selected_mod'), '_name')).GetFlagName"
}

# Args: TabKey (string)
# Returns: string
macro = {
    description = "Read tab owner mod id for a tab key"
    definition = "CMMTabOwnerModId(TabKey)"
    replace_with = "CMMVar(Concatenate('cmm_tab_owner_mod_id_', TabKey)).GetFlagName"
}

# Args: TabKey (string)
# Returns: string
macro = {
    description = "Tab label localization"
    definition = "CMMTabName(TabKey)"
    replace_with = "CMMVar(Concatenate(TabKey, '_name')).GetFlagName"
}

# Args: GroupKey (string)
# Returns: string
macro = {
    description = "Read group owner mod id for a group key"
    definition = "CMMGroupOwnerModId(GroupKey)"
    replace_with = "CMMVar(Concatenate('cmm_group_owner_mod_id_', GroupKey)).GetFlagName"
}

# Args: GroupKey (string)
# Returns: string
macro = {
    description = "Read group owner tab key for a group key"
    definition = "CMMGroupOwnerTabKey(GroupKey)"
    replace_with = "CMMVar(Concatenate('cmm_group_owner_tab_key_', GroupKey)).GetFlagName"
}

# Args: GroupKey (string)
# Returns: string
macro = {
    description = "Group label localization"
    definition = "CMMGroupName(GroupKey)"
    replace_with = "CMMVar(Concatenate(GroupKey, '_name')).GetFlagName"
}

# Args: GroupKey (string)
# Returns: boolean
macro = {
    description = "Visibility rule for one group row"
    definition = "CMMGroupRowVisible(GroupKey)"
    replace_with = "And(And(CMMSelectedModExists, GetVariableSystem.HasValue('cmm_selected_mod', CMMGroupOwnerModId(GroupKey))), And(CMMSelectedTabExists, GetVariableSystem.HasValue('cmm_selected_tab', CMMGroupOwnerTabKey(GroupKey))))"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Read setting owner mod id for a setting key"
    definition = "CMMSettingOwnerModId(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_setting_owner_mod_id_', SettingKey)).GetFlagName"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Read setting owner tab key for a setting key"
    definition = "CMMSettingOwnerTabKey(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_setting_owner_tab_key_', SettingKey)).GetFlagName"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key stores value in global scope"
    definition = "CMMSettingIsGlobal(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_setting_is_global_', SettingKey)).GetValue, '(CFixedPoint)1')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as text type"
    definition = "CMMSettingIsText(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_setting_text_character_limit_', SettingKey)).IsSet"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as slider type"
    definition = "CMMSettingIsSlider(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_setting_is_slider_', SettingKey)).GetValue, '(CFixedPoint)1')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as button type"
    definition = "CMMSettingIsButton(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_setting_is_button_', SettingKey)).GetValue, '(CFixedPoint)1')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as list type"
    definition = "CMMSettingIsList(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_setting_list_count_', SettingKey)).IsSet"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when this is the core host-only-tools gate setting"
    definition = "CMMIsHostOnlyToolsSetting(SettingKey)"
    replace_with = "EqualTo_string(SettingKey, 'cmm_core__enable_host_only_tools')"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when the core host-only-tools gate is enabled"
    definition = "CMMHostOnlyToolsEnabled"
    replace_with = "And(GetGlobalVariable('cmm_core__enable_host_only_tools').IsSet, EqualTo_CFixedPoint(GetGlobalVariable('cmm_core__enable_host_only_tools').GetValue, '(CFixedPoint)1'))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when local player can edit this setting (global settings are host-only in MP and gated by the core host-only-tools toggle)"
    definition = "CMMCanEditSetting(SettingKey)"
    replace_with = "Or(Not(CMMSettingIsGlobal(SettingKey)), And(Or(CMMIsHostOnlyToolsSetting(SettingKey), CMMHostOnlyToolsEnabled), Or(Not(GameIsMultiplayer), IsHost)))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as dropdown type"
    definition = "CMMSettingIsDropdown(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_setting_dropdown_count_', SettingKey)).IsSet"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as numeric type"
    definition = "CMMSettingIsNumeric(SettingKey)"
    replace_with = "And(CMMVar(Concatenate('cmm_setting_step_', SettingKey)).IsSet, Not(CMMSettingIsSlider(SettingKey)))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when a setting key is registered as bool type"
    definition = "CMMSettingIsBool(SettingKey)"
    replace_with = "And(And4(Not(CMMSettingIsNumeric(SettingKey)), Not(CMMSettingIsDropdown(SettingKey)), Not(CMMSettingIsSlider(SettingKey)), Not(CMMSettingIsText(SettingKey))), And(Not(CMMSettingIsButton(SettingKey)), Not(CMMSettingIsList(SettingKey))))"
}

# Args: SettingKey (string)
# Returns: value
macro = {
    description = "Read current setting value from country or global scope"
    definition = "CMMSettingValue(SettingKey)"
    replace_with = "Select_CFixedPoint(CMMSettingIsGlobal(SettingKey), GetGlobalVariable(SettingKey).GetValue, CMMVar(SettingKey).GetValue)"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Setting label localization"
    definition = "CMMSettingName(SettingKey)"
    replace_with = "CMMVar(Concatenate(SettingKey, '_name')).GetFlagName"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Setting description localization"
    definition = "CMMSettingDesc(SettingKey)"
    replace_with = "CMMVar(Concatenate(SettingKey, '_desc')).GetFlagName"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Ordered list item count for one setting"
    definition = "CMMListItemCount(SettingKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_setting_list_count_', SettingKey)).GetValue)"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Ordered list last valid position for one setting"
    definition = "CMMListLastIndex(SettingKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_setting_list_last_index_', SettingKey)).GetValue)"
}

# Args: ItemKey (string)
# Returns: string
macro = {
    description = "Owner setting key for one ordered list item key"
    definition = "CMMListItemOwnerSettingKey(ItemKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_item_owner_setting_', ItemKey)).GetFlagName"
}

# Args: ItemKey (string)
# Returns: string
macro = {
    description = "Ordered list item label localization"
    definition = "CMMListItemName(ItemKey)"
    replace_with = "CMMVar(Concatenate(ItemKey, '_name')).GetFlagName"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Ordered list field count for one setting"
    definition = "CMMListFieldCount(SettingKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_list_field_count_', SettingKey)).GetValue)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when an ordered list setting has at least one registered field"
    definition = "CMMListHasFields(SettingKey)"
    replace_with = "Not(EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_count_', SettingKey)).GetValue, '(CFixedPoint)0'))"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 0 exists"
    definition = "CMMListFieldHasSlot0(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_0_type_', SettingKey)).IsSet"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 1 exists"
    definition = "CMMListFieldHasSlot1(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_1_type_', SettingKey)).IsSet"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 2 exists"
    definition = "CMMListFieldHasSlot2(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_2_type_', SettingKey)).IsSet"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 3 exists"
    definition = "CMMListFieldHasSlot3(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_3_type_', SettingKey)).IsSet"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 4 exists"
    definition = "CMMListFieldHasSlot4(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_4_type_', SettingKey)).IsSet"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 0 is bool"
    definition = "CMMListFieldIsBool0(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_0_type_', SettingKey)).GetValue, '(CFixedPoint)1')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 1 is bool"
    definition = "CMMListFieldIsBool1(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_1_type_', SettingKey)).GetValue, '(CFixedPoint)1')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 2 is bool"
    definition = "CMMListFieldIsBool2(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_2_type_', SettingKey)).GetValue, '(CFixedPoint)1')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 3 is bool"
    definition = "CMMListFieldIsBool3(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_3_type_', SettingKey)).GetValue, '(CFixedPoint)1')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 4 is bool"
    definition = "CMMListFieldIsBool4(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_4_type_', SettingKey)).GetValue, '(CFixedPoint)1')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 0 is dropdown"
    definition = "CMMListFieldIsDropdown0(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_0_type_', SettingKey)).GetValue, '(CFixedPoint)2')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 1 is dropdown"
    definition = "CMMListFieldIsDropdown1(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_1_type_', SettingKey)).GetValue, '(CFixedPoint)2')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 2 is dropdown"
    definition = "CMMListFieldIsDropdown2(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_2_type_', SettingKey)).GetValue, '(CFixedPoint)2')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 3 is dropdown"
    definition = "CMMListFieldIsDropdown3(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_3_type_', SettingKey)).GetValue, '(CFixedPoint)2')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 4 is dropdown"
    definition = "CMMListFieldIsDropdown4(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_4_type_', SettingKey)).GetValue, '(CFixedPoint)2')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 0 is numeric"
    definition = "CMMListFieldIsNumeric0(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_0_type_', SettingKey)).GetValue, '(CFixedPoint)3')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 1 is numeric"
    definition = "CMMListFieldIsNumeric1(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_1_type_', SettingKey)).GetValue, '(CFixedPoint)3')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 2 is numeric"
    definition = "CMMListFieldIsNumeric2(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_2_type_', SettingKey)).GetValue, '(CFixedPoint)3')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 3 is numeric"
    definition = "CMMListFieldIsNumeric3(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_3_type_', SettingKey)).GetValue, '(CFixedPoint)3')"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when ordered list field slot 4 is numeric"
    definition = "CMMListFieldIsNumeric4(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_list_field_4_type_', SettingKey)).GetValue, '(CFixedPoint)3')"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Ordered list item-column header localization"
    definition = "CMMListItemColumnName(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_item_column_name_', SettingKey)).GetFlagName"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Ordered list field slot 0 header localization"
    definition = "CMMListFieldName0(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_0_name_', SettingKey)).GetFlagName"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Ordered list field slot 1 header localization"
    definition = "CMMListFieldName1(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_1_name_', SettingKey)).GetFlagName"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Ordered list field slot 2 header localization"
    definition = "CMMListFieldName2(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_2_name_', SettingKey)).GetFlagName"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Ordered list field slot 3 header localization"
    definition = "CMMListFieldName3(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_3_name_', SettingKey)).GetFlagName"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Ordered list field slot 4 header localization"
    definition = "CMMListFieldName4(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_4_name_', SettingKey)).GetFlagName"
}

# Args: ItemKey (string)
# Returns: boolean
macro = {
    description = "Checkbox state for ordered list field slot 0"
    definition = "CMMListFieldBoolChecked0(ItemKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate(ItemKey, '_field_0')).GetValue, '(CFixedPoint)1')"
}

# Args: ItemKey (string)
# Returns: boolean
macro = {
    description = "Checkbox state for ordered list field slot 1"
    definition = "CMMListFieldBoolChecked1(ItemKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate(ItemKey, '_field_1')).GetValue, '(CFixedPoint)1')"
}

# Args: ItemKey (string)
# Returns: boolean
macro = {
    description = "Checkbox state for ordered list field slot 2"
    definition = "CMMListFieldBoolChecked2(ItemKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate(ItemKey, '_field_2')).GetValue, '(CFixedPoint)1')"
}

# Args: ItemKey (string)
# Returns: boolean
macro = {
    description = "Checkbox state for ordered list field slot 3"
    definition = "CMMListFieldBoolChecked3(ItemKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate(ItemKey, '_field_3')).GetValue, '(CFixedPoint)1')"
}

# Args: ItemKey (string)
# Returns: boolean
macro = {
    description = "Checkbox state for ordered list field slot 4"
    definition = "CMMListFieldBoolChecked4(ItemKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate(ItemKey, '_field_4')).GetValue, '(CFixedPoint)1')"
}

# Args: ItemKey (string)
# Returns: string
macro = {
    description = "Localized dropdown label for ordered list field slot 0"
    definition = "CMMListFieldDropdownLabel0(ItemKey)"
    replace_with = "Localize(Concatenate(Concatenate(CMMVar(Concatenate('cmm_list_field_0_option_root_', CMMListItemOwnerSettingKey(ItemKey))).GetFlagName, '_option_'), Concatenate(ToString_int32(FixedPointToInt(CMMVar(Concatenate(ItemKey, '_field_0')).GetValue)), '_name')))"
}

# Args: ItemKey (string)
# Returns: string
macro = {
    description = "Localized dropdown label for ordered list field slot 1"
    definition = "CMMListFieldDropdownLabel1(ItemKey)"
    replace_with = "Localize(Concatenate(Concatenate(CMMVar(Concatenate('cmm_list_field_1_option_root_', CMMListItemOwnerSettingKey(ItemKey))).GetFlagName, '_option_'), Concatenate(ToString_int32(FixedPointToInt(CMMVar(Concatenate(ItemKey, '_field_1')).GetValue)), '_name')))"
}

# Args: ItemKey (string)
# Returns: string
macro = {
    description = "Localized dropdown label for ordered list field slot 2"
    definition = "CMMListFieldDropdownLabel2(ItemKey)"
    replace_with = "Localize(Concatenate(Concatenate(CMMVar(Concatenate('cmm_list_field_2_option_root_', CMMListItemOwnerSettingKey(ItemKey))).GetFlagName, '_option_'), Concatenate(ToString_int32(FixedPointToInt(CMMVar(Concatenate(ItemKey, '_field_2')).GetValue)), '_name')))"
}

# Args: ItemKey (string)
# Returns: string
macro = {
    description = "Localized dropdown label for ordered list field slot 3"
    definition = "CMMListFieldDropdownLabel3(ItemKey)"
    replace_with = "Localize(Concatenate(Concatenate(CMMVar(Concatenate('cmm_list_field_3_option_root_', CMMListItemOwnerSettingKey(ItemKey))).GetFlagName, '_option_'), Concatenate(ToString_int32(FixedPointToInt(CMMVar(Concatenate(ItemKey, '_field_3')).GetValue)), '_name')))"
}

# Args: ItemKey (string)
# Returns: string
macro = {
    description = "Localized dropdown label for ordered list field slot 4"
    definition = "CMMListFieldDropdownLabel4(ItemKey)"
    replace_with = "Localize(Concatenate(Concatenate(CMMVar(Concatenate('cmm_list_field_4_option_root_', CMMListItemOwnerSettingKey(ItemKey))).GetFlagName, '_option_'), Concatenate(ToString_int32(FixedPointToInt(CMMVar(Concatenate(ItemKey, '_field_4')).GetValue)), '_name')))"
}

# Args: ItemKey (string)
# Returns: int32
macro = {
    description = "Dropdown option count for ordered list field slot 0"
    definition = "CMMListFieldDropdownCount0(ItemKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_list_field_0_dropdown_count_', CMMListItemOwnerSettingKey(ItemKey))).GetValue)"
}

# Args: ItemKey (string)
# Returns: int32
macro = {
    description = "Dropdown option count for ordered list field slot 1"
    definition = "CMMListFieldDropdownCount1(ItemKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_list_field_1_dropdown_count_', CMMListItemOwnerSettingKey(ItemKey))).GetValue)"
}

# Args: ItemKey (string)
# Returns: int32
macro = {
    description = "Dropdown option count for ordered list field slot 2"
    definition = "CMMListFieldDropdownCount2(ItemKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_list_field_2_dropdown_count_', CMMListItemOwnerSettingKey(ItemKey))).GetValue)"
}

# Args: ItemKey (string)
# Returns: int32
macro = {
    description = "Dropdown option count for ordered list field slot 3"
    definition = "CMMListFieldDropdownCount3(ItemKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_list_field_3_dropdown_count_', CMMListItemOwnerSettingKey(ItemKey))).GetValue)"
}

# Args: ItemKey (string)
# Returns: int32
macro = {
    description = "Dropdown option count for ordered list field slot 4"
    definition = "CMMListFieldDropdownCount4(ItemKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_list_field_4_dropdown_count_', CMMListItemOwnerSettingKey(ItemKey))).GetValue)"
}

# Args: ItemKey (string)
# Returns: int32
macro = {
    description = "Selected dropdown index for ordered list field slot 0"
    definition = "CMMListFieldDropdownSelectedIndex0(ItemKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate(ItemKey, '_field_0')).GetValue)"
}

# Args: ItemKey (string)
# Returns: int32
macro = {
    description = "Selected dropdown index for ordered list field slot 1"
    definition = "CMMListFieldDropdownSelectedIndex1(ItemKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate(ItemKey, '_field_1')).GetValue)"
}

# Args: ItemKey (string)
# Returns: int32
macro = {
    description = "Selected dropdown index for ordered list field slot 2"
    definition = "CMMListFieldDropdownSelectedIndex2(ItemKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate(ItemKey, '_field_2')).GetValue)"
}

# Args: ItemKey (string)
# Returns: int32
macro = {
    description = "Selected dropdown index for ordered list field slot 3"
    definition = "CMMListFieldDropdownSelectedIndex3(ItemKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate(ItemKey, '_field_3')).GetValue)"
}

# Args: ItemKey (string)
# Returns: int32
macro = {
    description = "Selected dropdown index for ordered list field slot 4"
    definition = "CMMListFieldDropdownSelectedIndex4(ItemKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate(ItemKey, '_field_4')).GetValue)"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric value for ordered list field slot 0"
    definition = "CMMListFieldNumericValue0(ItemKey)"
    replace_with = "CMMVar(Concatenate(ItemKey, '_field_0')).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric value for ordered list field slot 1"
    definition = "CMMListFieldNumericValue1(ItemKey)"
    replace_with = "CMMVar(Concatenate(ItemKey, '_field_1')).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric value for ordered list field slot 2"
    definition = "CMMListFieldNumericValue2(ItemKey)"
    replace_with = "CMMVar(Concatenate(ItemKey, '_field_2')).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric value for ordered list field slot 3"
    definition = "CMMListFieldNumericValue3(ItemKey)"
    replace_with = "CMMVar(Concatenate(ItemKey, '_field_3')).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric value for ordered list field slot 4"
    definition = "CMMListFieldNumericValue4(ItemKey)"
    replace_with = "CMMVar(Concatenate(ItemKey, '_field_4')).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric minimum bound for ordered list field slot 0"
    definition = "CMMListFieldNumericMin0(ItemKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_0_min_', CMMListItemOwnerSettingKey(ItemKey))).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric minimum bound for ordered list field slot 1"
    definition = "CMMListFieldNumericMin1(ItemKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_1_min_', CMMListItemOwnerSettingKey(ItemKey))).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric minimum bound for ordered list field slot 2"
    definition = "CMMListFieldNumericMin2(ItemKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_2_min_', CMMListItemOwnerSettingKey(ItemKey))).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric minimum bound for ordered list field slot 3"
    definition = "CMMListFieldNumericMin3(ItemKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_3_min_', CMMListItemOwnerSettingKey(ItemKey))).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric minimum bound for ordered list field slot 4"
    definition = "CMMListFieldNumericMin4(ItemKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_4_min_', CMMListItemOwnerSettingKey(ItemKey))).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric maximum bound for ordered list field slot 0"
    definition = "CMMListFieldNumericMax0(ItemKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_0_max_', CMMListItemOwnerSettingKey(ItemKey))).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric maximum bound for ordered list field slot 1"
    definition = "CMMListFieldNumericMax1(ItemKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_1_max_', CMMListItemOwnerSettingKey(ItemKey))).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric maximum bound for ordered list field slot 2"
    definition = "CMMListFieldNumericMax2(ItemKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_2_max_', CMMListItemOwnerSettingKey(ItemKey))).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric maximum bound for ordered list field slot 3"
    definition = "CMMListFieldNumericMax3(ItemKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_3_max_', CMMListItemOwnerSettingKey(ItemKey))).GetValue"
}

# Args: ItemKey (string)
# Returns: value
macro = {
    description = "Numeric maximum bound for ordered list field slot 4"
    definition = "CMMListFieldNumericMax4(ItemKey)"
    replace_with = "CMMVar(Concatenate('cmm_list_field_4_max_', CMMListItemOwnerSettingKey(ItemKey))).GetValue"
}

# Args: ItemKey (string), Index (int32)
# Returns: string
macro = {
    description = "Localized dropdown list label for ordered list field slot 0"
    definition = "CMMListFieldDropdownOptionLabel0(ItemKey,Index)"
    replace_with = "Localize(Concatenate(Concatenate(CMMVar(Concatenate('cmm_list_field_0_option_root_', CMMListItemOwnerSettingKey(ItemKey))).GetFlagName, '_option_'), Concatenate(ToString_int32(Index), '_name')))"
}

# Args: ItemKey (string), Index (int32)
# Returns: string
macro = {
    description = "Localized dropdown list label for ordered list field slot 1"
    definition = "CMMListFieldDropdownOptionLabel1(ItemKey,Index)"
    replace_with = "Localize(Concatenate(Concatenate(CMMVar(Concatenate('cmm_list_field_1_option_root_', CMMListItemOwnerSettingKey(ItemKey))).GetFlagName, '_option_'), Concatenate(ToString_int32(Index), '_name')))"
}

# Args: ItemKey (string), Index (int32)
# Returns: string
macro = {
    description = "Localized dropdown list label for ordered list field slot 2"
    definition = "CMMListFieldDropdownOptionLabel2(ItemKey,Index)"
    replace_with = "Localize(Concatenate(Concatenate(CMMVar(Concatenate('cmm_list_field_2_option_root_', CMMListItemOwnerSettingKey(ItemKey))).GetFlagName, '_option_'), Concatenate(ToString_int32(Index), '_name')))"
}

# Args: ItemKey (string), Index (int32)
# Returns: string
macro = {
    description = "Localized dropdown list label for ordered list field slot 3"
    definition = "CMMListFieldDropdownOptionLabel3(ItemKey,Index)"
    replace_with = "Localize(Concatenate(Concatenate(CMMVar(Concatenate('cmm_list_field_3_option_root_', CMMListItemOwnerSettingKey(ItemKey))).GetFlagName, '_option_'), Concatenate(ToString_int32(Index), '_name')))"
}

# Args: ItemKey (string), Index (int32)
# Returns: string
macro = {
    description = "Localized dropdown list label for ordered list field slot 4"
    definition = "CMMListFieldDropdownOptionLabel4(ItemKey,Index)"
    replace_with = "Localize(Concatenate(Concatenate(CMMVar(Concatenate('cmm_list_field_4_option_root_', CMMListItemOwnerSettingKey(ItemKey))).GetFlagName, '_option_'), Concatenate(ToString_int32(Index), '_name')))"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Button setting button-text localization"
    definition = "CMMButtonSettingText(SettingKey)"
    replace_with = "CMMVar(Concatenate(SettingKey, '_button_text')).GetFlagName"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when selected tab has a tab-setting-count variable"
    definition = "CMMSelectedTabCountIsSet"
    replace_with = "CMMVar(Concatenate('cmm_tab_setting_count_', GetVariableSystem.Get('cmm_selected_tab'))).IsSet"
}

# Args: none
# Returns: value
macro = {
    description = "Selected tab setting count value"
    definition = "CMMSelectedTabCountValue"
    replace_with = "CMMVar(Concatenate('cmm_tab_setting_count_', GetVariableSystem.Get('cmm_selected_tab'))).GetValue"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when no mods are registered yet"
    definition = "CMMNoModsRegistered"
    replace_with = "IsDataModelEmpty(Player.MakeScope.GetList('cmm_registered_mod_ids'))"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when at least one mod is registered"
    definition = "CMMHasRegisteredMods"
    replace_with = "Not(IsDataModelEmpty(Player.MakeScope.GetList('cmm_registered_mod_ids')))"
}

# Args: ModId (string)
# Returns: boolean
macro = {
    description = "Filter match for one mod row against current search text"
    definition = "CMMModMatchesSearch(ModId)"
    replace_with = "Or4(Not(GetVariableSystem.Exists('cmm_mod_search_text')), StringIsEmpty(GetVariableSystem.Get('cmm_mod_search_text')), StringContains(CMMVar(Concatenate(ModId, '_name')).GetFlagName, GetVariableSystem.Get('cmm_mod_search_text')), StringContains(ModId, GetVariableSystem.Get('cmm_mod_search_text')))"
}

# Args: ModId (string)
# Returns: string
macro = {
    description = "Localized row label for one mod row"
    definition = "CMMModRowLabel(ModId)"
    replace_with = "CMMVar(Concatenate(ModId, '_name')).GetFlagName"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Visibility rule for one setting row"
    definition = "CMMSettingRowVisible(SettingKey)"
    replace_with = "And3(And(CMMSelectedModExists, GetVariableSystem.HasValue('cmm_selected_mod', CMMSettingOwnerModId(SettingKey))), And(CMMSelectedTabExists, GetVariableSystem.HasValue('cmm_selected_tab', CMMSettingOwnerTabKey(SettingKey))), Or(CMMSettingIsText(SettingKey), GetScriptedGui(Concatenate(SettingKey, '_on_changed')).IsShown(GuiScope.SetRoot(GetPlayer.MakeScope).End)))"
}

# Args: SettingKey (string)
# Returns: none
macro = {
    description = "Execute setting on-changed scripted GUI callback"
    definition = "CMMExecuteSettingChanged(SettingKey)"
    replace_with = "GetScriptedGui(Concatenate(SettingKey, '_on_changed')).Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: boolean
macro = {
    description = "True when text settings may be edited in the current session"
    definition = "CMMCanEditTextSetting"
    replace_with = "Not(GameIsMultiplayer)"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Text setting editbox character limit"
    definition = "CMMTextSettingCharacterLimit(SettingKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_setting_text_character_limit_', SettingKey)).GetValue)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "True when submitted text should be wrapped in double quotes"
    definition = "CMMTextSettingQuoteText(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMVar(Concatenate('cmm_setting_text_quote_', SettingKey)).GetValue, '(CFixedPoint)1')"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Variable-system cache key for one text setting input field"
    definition = "CMMTextSettingInputCacheKey(SettingKey)"
    replace_with = "Concatenate('cmm_text_input_', SettingKey)"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Scripted effect callback key for one text setting"
    definition = "CMMTextSettingCallbackKey(SettingKey)"
    replace_with = "Concatenate(SettingKey, '_on_changed')"
}

# Args: none
# Returns: none
macro = {
    description = "Mark pending numeric change as one-step decrease"
    definition = "CMMMarkNumericDecrease"
    replace_with = "GetScriptedGui('CMM_MarkNumericDecrease').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark pending numeric change as five-step decrease"
    definition = "CMMMarkNumericDecrease5x"
    replace_with = "GetScriptedGui('CMM_MarkNumericDecrease5x').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark pending numeric change as jump-to-min"
    definition = "CMMMarkNumericDecreaseToMin"
    replace_with = "GetScriptedGui('CMM_MarkNumericDecreaseToMin').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark pending numeric change as one-step increase"
    definition = "CMMMarkNumericIncrease"
    replace_with = "GetScriptedGui('CMM_MarkNumericIncrease').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark pending numeric change as five-step increase"
    definition = "CMMMarkNumericIncrease5x"
    replace_with = "GetScriptedGui('CMM_MarkNumericIncrease5x').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark pending numeric change as jump-to-max"
    definition = "CMMMarkNumericIncreaseToMax"
    replace_with = "GetScriptedGui('CMM_MarkNumericIncreaseToMax').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: Index (int32)
# Returns: none
macro = {
    description = "Mark the next ordered list interaction with one row position"
    definition = "CMMMarkListPosition(Index)"
    replace_with = "GetScriptedGui(Concatenate('CMM_MarkListPosition_', ToString_int32(Index))).Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction as move-up"
    definition = "CMMMarkListMoveUp"
    replace_with = "GetScriptedGui('CMM_MarkListMoveUp').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction as move-down"
    definition = "CMMMarkListMoveDown"
    replace_with = "GetScriptedGui('CMM_MarkListMoveDown').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction as move-to-top"
    definition = "CMMMarkListMoveToTop"
    replace_with = "GetScriptedGui('CMM_MarkListMoveToTop').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction as move-to-bottom"
    definition = "CMMMarkListMoveToBottom"
    replace_with = "GetScriptedGui('CMM_MarkListMoveToBottom').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction with the cached hovered row position"
    definition = "CMMMarkHoveredListPosition"
    replace_with = "GetScriptedGui(Concatenate('CMM_MarkListPosition_', GetVariableSystem.Get('cmm_list_hover_index'))).Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction with field slot 0"
    definition = "CMMMarkListFieldSlot0"
    replace_with = "GetScriptedGui('CMM_MarkListFieldSlot_0').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction with field slot 1"
    definition = "CMMMarkListFieldSlot1"
    replace_with = "GetScriptedGui('CMM_MarkListFieldSlot_1').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction with field slot 2"
    definition = "CMMMarkListFieldSlot2"
    replace_with = "GetScriptedGui('CMM_MarkListFieldSlot_2').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction with field slot 3"
    definition = "CMMMarkListFieldSlot3"
    replace_with = "GetScriptedGui('CMM_MarkListFieldSlot_3').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction with field slot 4"
    definition = "CMMMarkListFieldSlot4"
    replace_with = "GetScriptedGui('CMM_MarkListFieldSlot_4').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction as a bool-field toggle"
    definition = "CMMMarkListFieldToggle"
    replace_with = "GetScriptedGui('CMM_MarkListFieldToggle').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction as toggling one bool field for all rows"
    definition = "CMMMarkListFieldToggleAll"
    replace_with = "GetScriptedGui('CMM_MarkListFieldToggleAll').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction as selecting a dropdown-field option"
    definition = "CMMMarkListFieldSelectDropdown"
    replace_with = "GetScriptedGui('CMM_MarkListFieldSelectDropdown').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction as selecting a dropdown-field option for all rows"
    definition = "CMMMarkListFieldSelectDropdownAll"
    replace_with = "GetScriptedGui('CMM_MarkListFieldSelectDropdownAll').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction as a dropdown-field next cycle"
    definition = "CMMMarkListFieldCycleNext"
    replace_with = "GetScriptedGui('CMM_MarkListFieldCycleNext').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "Mark the next ordered list interaction as a dropdown-field previous cycle"
    definition = "CMMMarkListFieldCyclePrev"
    replace_with = "GetScriptedGui('CMM_MarkListFieldCyclePrev').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: SettingKey (string)
# Returns: boolean
macro = {
    description = "Checkbox checked state for bool settings (1 = on)"
    definition = "CMMIsSettingEnabled(SettingKey)"
    replace_with = "EqualTo_CFixedPoint(CMMSettingValue(SettingKey), '(CFixedPoint)1')"
}

# Args: SettingKey (string)
# Returns: value
macro = {
    description = "Numeric setting value text"
    definition = "CMMNumericSettingValue(SettingKey)"
    replace_with = "CMMSettingValue(SettingKey)"
}

# Args: SettingKey (string)
# Returns: value
macro = {
    description = "Numeric setting minimum bound text"
    definition = "CMMNumericSettingMin(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_setting_min_', SettingKey)).GetValue"
}

# Args: SettingKey (string)
# Returns: value
macro = {
    description = "Numeric setting maximum bound text"
    definition = "CMMNumericSettingMax(SettingKey)"
    replace_with = "CMMVar(Concatenate('cmm_setting_max_', SettingKey)).GetValue"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Slider visual step count for one setting"
    definition = "CMMSliderVisualStepCount(SettingKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_setting_slider_visual_step_count_', SettingKey)).GetValue)"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Slider actual last index for one setting"
    definition = "CMMSliderActualLastIndex(SettingKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_setting_slider_actual_last_index_', SettingKey)).GetValue)"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Slider visual last index for one setting"
    definition = "CMMSliderVisualLastIndex(SettingKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_setting_slider_visual_last_index_', SettingKey)).GetValue)"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Current actual slider step index for one setting"
    definition = "CMMSliderCurrentActualIndex(SettingKey)"
    replace_with = "FixedPointToInt(Add_CFixedPoint(Divide_CFixedPoint(Subtract_CFixedPoint(CMMSettingValue(SettingKey), CMMVar(Concatenate('cmm_setting_min_', SettingKey)).GetValue), CMMVar(Concatenate('cmm_setting_step_', SettingKey)).GetValue), '(CFixedPoint)0.5'))"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Current visual slider slot index for one setting"
    definition = "CMMSliderCurrentVisualIndex(SettingKey)"
    replace_with = "Divide_int32(Multiply_int32(CMMSliderCurrentActualIndex(SettingKey), CMMSliderVisualLastIndex(SettingKey)), CMMSliderActualLastIndex(SettingKey))"
}

# Args: none
# Returns: none
macro = {
    description = "Begin a slider drag interaction"
    definition = "CMMStartSliderDrag"
    replace_with = "GetScriptedGui('CMM_StartSliderDrag').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: none
# Returns: none
macro = {
    description = "End a slider drag interaction"
    definition = "CMMStopSliderDrag"
    replace_with = "GetScriptedGui('CMM_StopSliderDrag').Execute(GuiScope.SetRoot(GetPlayer.MakeScope).End)"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Dropdown option count for one setting"
    definition = "CMMDropdownOptionCount(SettingKey)"
    replace_with = "FixedPointToInt(CMMVar(Concatenate('cmm_setting_dropdown_count_', SettingKey)).GetValue)"
}

# Args: SettingKey (string)
# Returns: int32
macro = {
    description = "Dropdown selected index for one setting"
    definition = "CMMDropdownSelectedIndex(SettingKey)"
    replace_with = "FixedPointToInt(CMMSettingValue(SettingKey))"
}

# Args: SettingKey (string)
# Returns: string
macro = {
    description = "Localized selected dropdown label for one setting"
    definition = "CMMDropdownSelectedLabel(SettingKey)"
    replace_with = "Localize(Concatenate(Concatenate(SettingKey, '_option_'), Concatenate(ToString_int32(CMMDropdownSelectedIndex(SettingKey)), '_name')))"
}

# Args: SettingKey (string), Index (int32)
# Returns: string
macro = {
    description = "Localized dropdown list label for one setting and option index"
    definition = "CMMDropdownListLabel(SettingKey,Index)"
    replace_with = "Localize(Concatenate(Concatenate(SettingKey, '_option_'), Concatenate(ToString_int32(Index), '_name')))"
}

# Args: none
# Returns: boolean
macro = {
    description = "Show empty-state text when selected tab has zero visible settings"
    definition = "CMMShowNoSettingsForSelectedTab"
    replace_with = "And(CMMSelectedModExists, Or3(Not(CMMSelectedTabExists), Not(CMMSelectedTabCountIsSet), EqualTo_CFixedPoint(CMMSelectedTabCountValue, '(CFixedPoint)0')))"
}


