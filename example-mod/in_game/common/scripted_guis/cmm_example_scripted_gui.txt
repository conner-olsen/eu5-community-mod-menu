cmm_example__feature_enabled_on_changed = {
	scope = country

	effect = {
		cmm_toggle_bool_setting = {
			setting = cmm_example__feature_enabled
		}

		if = {
			limit = { var:cmm_example__feature_enabled = 1 }
			# enabled effect
		}
		else = {
			# disabled effect
		}
	}
}

cmm_example__show_notifications_on_changed = {
	scope = country

	effect = {
		cmm_toggle_bool_setting = {
			setting = cmm_example__show_notifications
		}

		if = {
			limit = { var:cmm_example__show_notifications = 1 }
			# enabled effect
		}
		else = {
			# disabled effect
		}
	}
}
