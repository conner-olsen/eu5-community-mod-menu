CMMExample_ToggleEnabled = {
    effect = {
        if = {
            limit = { NOT = { has_variable = cmm_example_setting_enabled } }
            set_variable = { name = cmm_example_setting_enabled value = 1 }
        }
        else = {
            remove_variable = cmm_example_setting_enabled
        }
    }
}