# Root scope: country.
cmm_example_register_country = {
	cmm_register_mod = {
		mod_id = cmm_example
	}

	cmm_register_bool_setting = {
		mod_id = cmm_example
		setting_id = feature_enabled
		default_value = 1
	}

	cmm_register_bool_setting = {
		mod_id = cmm_example
		setting_id = show_notifications
		default_value = 0
	}
}

cmm_example__feature_enabled_on_changed = {
	if = {
		limit = {
			var:$setting$ = 1
		}
		# enabled effect
	}
	else = {
		# disabled effect
	}
}

cmm_example__show_notifications_on_changed = {
	if = {
		limit = {
			var:$setting$ = 1
		}
		# enabled effect
	}
	else = {
		# disabled effect
	}
}
